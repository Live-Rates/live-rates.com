# Python streaming client

```bash
pip install -r requirements.txt
LIVERATES_KEY=trial python client.py
```

Uses `python-socketio` v5 (compatible with the socket.io v4 server). TLS verification is on by default — don't disable it.
