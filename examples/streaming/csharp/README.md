# C# / .NET streaming client

Targets .NET 8 and uses [SocketIOClient](https://github.com/doghappy/socket.io-client-csharp) 3.x.

```bash
dotnet run
```

Set `LIVERATES_KEY=trial` (or your real key) in the environment before running.
