# PHP streaming client

Uses [elephant.io](https://github.com/ElephantIO/elephant.io) 5.x (socket.io v4 compatible).

```bash
composer install
LIVERATES_KEY=trial php client.php
```

The old bundled `vendor/` tree has been removed — dependencies are pulled via composer.
